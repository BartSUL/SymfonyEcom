{% extends 'base.html.twig' %}

{% block title %}Accueil - {{ parent() }}{% endblock %}

{% block body %}
	<!-- The Band Section -->
	<div class="w3-container w3-content w3-center w3-padding-64" style="max-width:800px" id="dataform">
		<h2 class="w3-wide">{{ formName }}</h2>
		<div>
			{{ form(dataForm)}}
		</div>
	</div>
{% endblock %}
